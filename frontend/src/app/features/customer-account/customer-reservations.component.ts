import { Component, OnInit, inject, signal } from '@angular/core';import { MatButtonModule } from '@angular/material/button';import { MatIconModule } from '@angular/material/icon';import { RouterLink } from '@angular/router';import { formatTryAmount } from '../../core/format/currency.util';import { CustomerReservation } from '../marketplace/marketplace.models';import { MarketplaceService } from '../marketplace/marketplace.service';
@Component({selector:'acr-customer-reservations',imports:[MatButtonModule,MatIconModule,RouterLink],template:`<section class="bookings"><div class="heading"><div><h1>My bookings</h1><p>Reservations created while signed in appear here.</p></div><div><a mat-stroked-button routerLink="/customer/account/profile"><mat-icon>person</mat-icon>Profile</a><a mat-flat-button color="primary" routerLink="/rent"><mat-icon>add</mat-icon>New booking</a></div></div>@if(loading()){<div class="state">Loading bookings...</div>}@else if(error()){<div class="state error">{{error()}}</div>}@else if(items().length===0){<div class="state"><mat-icon>event_busy</mat-icon><strong>No account bookings yet</strong><span>Guest bookings remain available through reservation tracking.</span></div>}@else{<div class="list">@for(item of items();track item.reservationCode){<article><div><span class="tenant">{{item.tenantName}}</span><h2>{{item.vehicleBrand}} {{item.vehicleModel}}</h2><p>{{date(item.pickupDateTime)}} → {{date(item.returnDateTime)}}</p></div><div class="amount"><strong>{{money(item.totalEstimatedPrice)}}</strong><span>{{item.paymentStatusSummary}}</span></div><span class="status">{{item.status}}</span><a mat-stroked-button [routerLink]="['/customer/account/reservations',item.reservationCode]">Details</a></article>}</div>}</section>`,styles:[`.bookings{display:flex;flex-direction:column;gap:18px}.heading{display:flex;justify-content:space-between;align-items:flex-start}.heading h1{margin:0 0 4px}.heading p{color:#68758a;margin:0}.heading>div:last-child{display:flex;gap:8px}.list{display:flex;flex-direction:column;gap:10px}.list article{background:#fff;border:1px solid #dce2e9;border-radius:8px;padding:17px;display:grid;grid-template-columns:1fr auto auto auto;align-items:center;gap:22px}.tenant{font-size:11px;color:#1261b5;font-weight:700;text-transform:uppercase}.list h2{font-size:17px;margin:4px 0}.list p{margin:0;color:#68758a;font-size:13px}.amount{display:flex;flex-direction:column;text-align:right}.amount span{font-size:11px;color:#68758a}.status{background:#eef3f8;padding:6px 9px;border-radius:4px;font-size:11px;font-weight:700}.state{min-height:180px;background:#fff;border:1px solid #dce2e9;border-radius:8px;display:flex;align-items:center;justify-content:center;flex-direction:column;gap:6px;color:#68758a}.state.error{color:#a11c1c}@media(max-width:720px){.heading{flex-direction:column;gap:12px}.list article{grid-template-columns:1fr auto}.amount{text-align:left}.status{justify-self:start}}`]})
export class CustomerReservationsComponent implements OnInit{private readonly service=inject(MarketplaceService);readonly items=signal<CustomerReservation[]>([]);readonly loading=signal(true);readonly error=signal('');ngOnInit():void{this.service.customerReservations().subscribe({next:v=>this.items.set(v),error:e=>{this.error.set(e.error?.message||'Bookings could not be loaded.');this.loading.set(false)},complete:()=>this.loading.set(false)})}money(v:number):string{return formatTryAmount(v)}date(v:string):string{return new Date(v).toLocaleString('en-GB',{dateStyle:'medium',timeStyle:'short'})}}
