import { Component, OnInit, inject, signal } from '@angular/core';import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';import { MatButtonModule } from '@angular/material/button';import { MatFormFieldModule } from '@angular/material/form-field';import { MatInputModule } from '@angular/material/input';import { CustomerProfile } from '../../core/customer-auth/customer-auth.models';import { MarketplaceService } from '../marketplace/marketplace.service';
@Component({selector:'acr-customer-profile',imports:[ReactiveFormsModule,MatButtonModule,MatFormFieldModule,MatInputModule],template:`<section class="profile"><div><h1>Profile</h1><p>Manage the contact information used for account bookings.</p></div>@if(error()){<div class="error">{{error()}}</div>}<form [formGroup]="form" (ngSubmit)="save()"><div class="row"><mat-form-field appearance="outline"><mat-label>First name</mat-label><input matInput formControlName="firstName"></mat-form-field><mat-form-field appearance="outline"><mat-label>Last name</mat-label><input matInput formControlName="lastName"></mat-form-field></div><mat-form-field appearance="outline"><mat-label>Email</mat-label><input matInput formControlName="email" readonly></mat-form-field><mat-form-field appearance="outline"><mat-label>Phone</mat-label><input matInput formControlName="phone"></mat-form-field><button mat-flat-button color="primary" [disabled]="saving()">Save changes</button>@if(saved()){<span class="saved">Profile updated</span>}</form></section>`,styles:[`.profile{max-width:720px;margin:20px auto}.profile>div:first-child h1{margin-bottom:5px}.profile>div:first-child p{color:#68758a}.profile form{background:#fff;border:1px solid #dce2e9;border-radius:8px;padding:24px;display:flex;flex-direction:column}.row{display:grid;grid-template-columns:1fr 1fr;gap:12px}.profile button{align-self:flex-start}.saved{color:#267b3f;margin:12px 0}.error{background:#fff1f1;color:#a11c1c;padding:10px;margin:12px 0}@media(max-width:560px){.row{grid-template-columns:1fr}}`]})
export class CustomerProfileComponent implements OnInit{private readonly fb=inject(FormBuilder);private readonly service=inject(MarketplaceService);readonly error=signal('');readonly saving=signal(false);readonly saved=signal(false);readonly form=this.fb.group({firstName:['',Validators.required],lastName:['',Validators.required],email:[''],phone:['',Validators.required]});ngOnInit():void{this.service.profile().subscribe({next:p=>this.form.patchValue(p),error:e=>this.error.set(e.error?.message||'Profile could not be loaded.')})}save():void{if(this.form.invalid)return;this.saving.set(true);this.saved.set(false);const v=this.form.getRawValue();this.service.updateProfile({firstName:v.firstName,lastName:v.lastName,phone:v.phone}).subscribe({next:(p:CustomerProfile)=>{this.form.patchValue(p);this.saved.set(true)},error:e=>{this.error.set(e.error?.message||'Profile could not be updated.');this.saving.set(false)},complete:()=>this.saving.set(false)})}}
